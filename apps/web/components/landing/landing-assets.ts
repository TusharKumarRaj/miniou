/** Drop your production images here — placeholders show until files exist. */
export const LANDING_ASSETS = {
    /** Hero: cyborg figure (dark mode). SVG in public/landing/. */
    heroFigure: "/landing/hero-figure.svg",
    /** Hero: cyborg figure (light mode). SVG in public/landing/. */
    heroFigureLight: "/landing/hero-figure-light.svg",
    /** Hero widget: small silhouette in white frame. PNG ~200×280. */
    heroThumb: "/landing/hero-thumb.png",
    /** Task section: person on ridge, misty sky → black. JPG/PNG ~1920×1080. */
    ridgeLandscape: "/landing/ridge-landscape.jpg",
    /** Vision section: figure in water, metallic bands. PNG ~1400×900. */
    waterFigure: "/landing/water-figure.png",
} as const;
